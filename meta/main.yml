---
allow_duplicates: no

dependencies:
  - { role: andyceo.preconf }

galaxy_info:
  author: andyceo
  description: Install nginx web server (nginx-naxsi), manage virtual hosts and settings for them
  license: "license (BSD, MIT)"
  min_ansible_version: 1.5
  platforms:
  - name: Ubuntu
    versions:
    - trusty
  categories:
    - web
    - system
